adds = (a, b) => a + b

subtracts = (a, b) => a - b

multiply = (a, b) => a * b

divide = (a, b) => a / b

inc = (a) => a++

dec = (a) => a--

makeInt = (n) => parseInt(n, 10)

preserveDecimal = (n) => parseFloat(n)
