add = (a, b) => a + b

subtract = (a, b) => a - b

multiply = (a, b) => a * b

divide = (a, b) => a / b

inc = (n) => n++

dec = (n) => n--

makeInt = (n) => parseInt(n, 10)

preserveDecimal = (n) => parseFloat(n)
